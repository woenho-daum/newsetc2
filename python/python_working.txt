고정/쉬프트 자료를 디비로 올리는 프로그램
1. dispatch_settings_cdp.py
2. collect_dispatch_settings.py

기사별 월별 근무현황을 디비로 올리는 프로그램
1. dispatch_driver_baecha_to_db.py		웹에서 조회하여 디비로 로드
2. dispatch_driver_baecha_to_excel.py	디비에서 지정된 일자부터 조회해서 엑셀로 저장

내보내기로 받은 2.1버전 vcard 파일을 디코딩하여 디비로 올리는 프로그램
1. insert_vcf_to_sqlite.py

디비에서 쿼리로 고정쉬프트와 전화번호 연결하고 csv로 내려받은 파일을 필요에 따라 손으로 편집한다.
그 다음 그 파일을 다시 vcf 파일로 만드는 프로그램
1. excel_to_vcf.py		: 버전 2.1용으로 만든다
2. excel_to_vcf_v3.py 	: 버전 3.0용으로 만든다
3. sort_cvf_v3.py 		: 버전 3.0은 UTF-8포맷으로 이름이 있으므로 소트할 수 있다

데이터베이스:
1. baecha.db 고정/쉬프트 자료 + 전화번호
2. dispatch.db 기사별 월별 근무현황(상세 및 집계)



=================================================================
chrome url로 호출방법 : http://127.0.0.1:9222/json/version

파이썬 인터프리터로 호출 방법
{
    import urllib.request
    print(urllib.request.urlopen("http://127.0.0.1:9222/json/version").read().decode())
}

아예 다름 환경의 크롬 기동하기
{
    "C:\Program Files\Google\Chrome\Application\chrome.exe"
        --remote-debugging-port=9222
        --remote-allow-origins=*
        --user-data-dir=C:\ChromeAHK
        --profile-directory=ProfileAHK
    "C:\Program Files\Google\Chrome\Application\chrome.exe"
        --remote-debugging-port=9223
        --remote-allow-origins=*
        --user-data-dir=C:\ChromePython
        --profile-directory=ProfilePython
}

pip 현재 .venv에 설치된 패키지 목록 백업/복원
{
    pip freeze > requirements.txt
    pip install -r requirements.txt
}

