\HKEY_CURRENT_USER\Software\Microsoft\Office\12.0\Excel\Security
Application.OnKey "{DELETE}"
Application.OnKey key, macroName
Application.OnKey "{DELETE}" "DeleteKeyHandler"


1. Excel에서 VBA 프로젝트에 대한 프로그래밍 방식 액세스가 허용되어 있어야 합니다.
	Excel에서:
		파일 → 옵션 → 보안 센터 → 보안 센터 설정 → 매크로 설정에서 {VBA 프로젝트 개체 모델에 안전하게 액세스할 수 있음 } 을 체크합니다.

2. 해당 작업 폴더를 만든다
3. 기존 .xlsm을 복사한다
4. XVBA에서 Excel 파일을 연결한다
	VS Code 왼쪽의 XVBA 아이콘을 보면 Excel/VBA 프로젝트와 관련된 메뉴가 나타납니다.
	여기서 핵심은 Import입니다.
	기존 Excel 파일의 VBA 프로젝트를 VS Code 쪽으로 가져옵니다.

{
	1. 먼저 Bootstrap XVBA Config를 실행
		현재 명령 팔레트에 { Bootstrap XVBA Config }를 실행하세요.
		그러면 Create Excel VBA Project 같은 선택지가 나올 것입니다.
	2.XVBA 공식 문서상 Bootstrap은 workspace 루트에 config.json을 만들고, 여기에서 Excel 파일과 VBA 폴더를 지정합니다.

}